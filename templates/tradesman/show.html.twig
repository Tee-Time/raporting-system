{% extends 'base.html.twig' %}

{% block title %}Tradesman{% endblock %}

{% block body %}
<div class="container">
    <h1>Tradesman</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tradesman.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tradesman.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tradesman_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('app_tradesman_edit', {'id': tradesman.id}) }}" class="btn btn-secondary">edit</a>

    {{ include('tradesman/_delete_form.html.twig') }}
</div>
{% endblock %}

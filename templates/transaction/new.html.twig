{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('app') }}
{% endblock %}

{% block title %}New Transaction{% endblock %}

{% block body %}
<div class="container">
    <div class="container">
        <h1>Create New Transaction</h1>

        {{ include('transaction/_form.html.twig') }}

        <a href="{{ path('app_transaction_index') }}" class="btn btn-primary">Back to List</a>
    </div>
</div>
{% endblock %}

